import React from "react";
import LoginComponent from "../objects/login/LoginComponent";

const Login = (props) => {
  return (
    <div>
      <LoginComponent />
    </div>
  );
};

export default Login;
